-- Add group-related columns to singsing_participants
ALTER TABLE singsing_participants 
ADD COLUMN IF NOT EXISTS group_size INTEGER DEFAULT 1,
ADD COLUMN IF NOT EXISTS is_paying_for_group BOOLEAN DEFAULT FALSE,
ADD COLUMN IF NOT EXISTS companions TEXT[];

-- Add payment-related columns for future use
ALTER TABLE singsing_participants 
ADD COLUMN IF NOT EXISTS deposit_amount INTEGER DEFAULT 0,
ADD COLUMN IF NOT EXISTS deposit_paid BOOLEAN DEFAULT FALSE,
ADD COLUMN IF NOT EXISTS deposit_paid_date TIMESTAMP WITH TIME ZONE,
ADD COLUMN IF NOT EXISTS balance_amount INTEGER DEFAULT 0,
ADD COLUMN IF NOT EXISTS balance_paid BOOLEAN DEFAULT FALSE,
ADD COLUMN IF NOT EXISTS balance_paid_date TIMESTAMP WITH TIME ZONE;
